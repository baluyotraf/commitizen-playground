[tool.commitizen]
name = "cz_conventional_commits"
version = "0.1.0"
version_provider = "commitizen"
version_files = [
  "repo-a/pyproject.toml:tool.commitizen.version",
  "repo-b/pyproject.toml:tool.commitizen.version",
]
tag_format = "v$version"
annotated_tag = true
update_changelog_on_bump = false
